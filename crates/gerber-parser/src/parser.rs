use crate::types::PointCommand;
use pcb_core::*;

pub fn parse_commands(_input: &str) -> Vec<PointCommand> {
    vec![]
}

pub fn process_moves(_commands: Vec<PointCommand>) -> Vec<TraceSegment> {
    vec![]
}